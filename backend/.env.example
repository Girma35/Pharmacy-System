# Backend Server Port
PORT=5000

# JSON Web Token Secret Key
JWT_SECRET=pharmacy-secure-jwt-key-2026

# PostgreSQL Connection Credentials
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_NAME=pharmacy

# Alternative Connection String (Optional - e.g. for Render or AWS RDS)
# DATABASE_URL=postgres://user:password@host:port/database
